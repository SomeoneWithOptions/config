# Application bindings
bindd = SUPER, RETURN, Terminal, exec, uwsm-app -- xdg-terminal-exec --dir="$(omarchy-cmd-terminal-cwd)"
bindd = SUPER ALT, RETURN, Tmux, exec, uwsm-app -- xdg-terminal-exec --dir="$(omarchy-cmd-terminal-cwd)" bash -c "tmux attach || tmux new -s Work"
bindd = SUPER SHIFT, RETURN, Browser, exec, omarchy-launch-browser
bindd = SUPER SHIFT, F, File manager, exec, uwsm-app -- nautilus --new-window
bindd = SUPER ALT SHIFT, F, File manager (cwd), exec, uwsm-app -- nautilus --new-window "$(omarchy-cmd-terminal-cwd)"
bindd = SUPER SHIFT, B, Browser, exec, omarchy-launch-browser
bindd = SUPER SHIFT ALT, B, Browser (private), exec, omarchy-launch-browser --private
bindd = SUPER SHIFT, M, Music, exec, omarchy-launch-or-focus spotify
bindd = SUPER SHIFT ALT, M, Music TUI, exec, omarchy-launch-or-focus-tui cliamp
bindd = SUPER SHIFT, N, Editor, exec, omarchy-launch-editor
bindd = SUPER SHIFT, D, Docker, exec, omarchy-launch-tui lazydocker
bindd = SUPER SHIFT, G, Signal, exec, omarchy-launch-or-focus ^signal$ "uwsm-app -- signal-desktop"
bindd = SUPER SHIFT, O, Obsidian, exec, omarchy-launch-or-focus ^obsidian$ "uwsm-app -- obsidian"
bindd = SUPER SHIFT, W, Typora, exec, uwsm-app -- typora --enable-wayland-ime
bindd = SUPER SHIFT, SLASH, Passwords, exec, uwsm-app -- 1password

# If your web app url contains #, type it as ## to prevent hyprland treating it as a comment
bindd = SUPER SHIFT, A, ChatGPT, exec, omarchy-launch-webapp "https://chatgpt.com"
bindd = SUPER SHIFT ALT, A, Grok, exec, omarchy-launch-webapp "https://grok.com"
bindd = SUPER SHIFT, C, Calendar, exec, omarchy-launch-webapp "https://app.hey.com/calendar/weeks/"
bindd = SUPER SHIFT, E, Email, exec, omarchy-launch-webapp "https://app.hey.com"
bindd = SUPER SHIFT, Y, YouTube, exec, omarchy-launch-webapp "https://youtube.com/"
bindd = SUPER SHIFT ALT, G, WhatsApp, exec, omarchy-launch-or-focus-webapp WhatsApp "https://web.whatsapp.com/"
bindd = SUPER SHIFT CTRL, G, Google Messages, exec, omarchy-launch-or-focus-webapp "Google Messages" "https://messages.google.com/web/conversations"
bindd = SUPER SHIFT, P, Google Photos, exec, omarchy-launch-or-focus-webapp "Google Photos" "https://photos.google.com/"
bindd = SUPER SHIFT, X, X, exec, omarchy-launch-webapp "https://x.com/"
bindd = SUPER SHIFT ALT, X, X Post, exec, omarchy-launch-webapp "https://x.com/compose/post"

# Add extra bindings
# bind = SUPER SHIFT, R, exec, alacritty -e ssh your-server
bindd = SUPER SHIFT, H, System monitor (btop), exec, omarchy-launch-or-focus org.omarchy.btop "alacritty --class org.omarchy.btop,org.omarchy.btop -e btop"

# Laptop lid behavior: when the lid closes with an external monitor connected,
# disable the internal panel and use only the external monitor. Re-enable the
# laptop panel when the lid opens.
bindl = , switch:on:Lid Switch, exec, ~/.local/bin/lid-monitor-mode close
bindl = , switch:off:Lid Switch, exec, ~/.local/bin/lid-monitor-mode open

# Overwrite existing bindings, like putting Omarchy Menu on Super + Space
# unbind = SUPER, SPACE
# bindd = SUPER, SPACE, Omarchy menu, exec, omarchy-menu

# Letter workspaces
# SUPER+F was fullscreen; SUPER+C was universal copy; SUPER+S was scratchpad.
# SUPER+SHIFT+F was file manager; SUPER+SHIFT+C was Calendar.
unbind = SUPER, F
unbind = SUPER, C
unbind = SUPER, S
unbind = SUPER SHIFT, F
unbind = SUPER SHIFT, C
unbind = SUPER SHIFT, S
bindd = SUPER, F, Switch to workspace F, workspace, name:F
bindd = SUPER, C, Switch to workspace C, workspace, name:C
bindd = SUPER, S, Switch to workspace S, workspace, name:S
bindd = SUPER SHIFT, F, Move window to workspace F, movetoworkspace, name:F
bindd = SUPER SHIFT, C, Move window to workspace C, movetoworkspace, name:C
bindd = SUPER SHIFT, S, Move window to workspace S, movetoworkspace, name:S

# Screenshot clipboard behavior
# PRINT was Omarchy's default screenshot binding. Override it so screenshots are
# copied like selecting the PNG in Files and pressing Alt+C.
unbind = , PRINT
bindd = , PRINT, Screenshot copied as file, exec, ~/.local/bin/omarchy-screenshot-file-clipboard

# Logitech MX Keys
# bind = SUPER SHIFT, S, exec, omarchy-capture-screenshot      # Print Screen Button
# bind = SUPER, H, exec, voxtype record toggle                 # Dictation Button
# bind = SUPER, PERIOD, exec, omarchy-launch-walker -m symbols # Emoji Button

# Mac-style editing shortcuts: Alt mirrors Ctrl for undo/cut/copy/paste
bindd = ALT, Z, Undo, sendshortcut, CTRL, Z, activewindow
bindd = ALT, X, Cut, sendshortcut, CTRL, X, activewindow
bindd = ALT, C, Copy, exec, ~/.local/bin/alt-edit-shortcut copy
bindd = ALT, V, Paste, exec, ~/.local/bin/alt-edit-shortcut paste
